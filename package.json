{
  "name": "pong",
  "version": "1.0.0",
  "private": true,
  "description": "Juego clásico de Pong con WebComponents nativos y Canvas 2D",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "servor src --reload",
    "test": "node --test",
    "test:watch": "node --test --watch",
    "deploy": "gh-pages -d src/"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "servor": "^4.0.2"
  }
}
